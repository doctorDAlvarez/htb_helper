curl -O [TARGET] #save output to default file name
curl -o FILENAME [target]
curl -s #silent mode
curl -k #skip SSL certificate verification
curl -i -I #Include Headers from response
curl -H #add headers to request.
curl -A #add User-agent header
curl -L #follow redirection
curl -X METHOD
curl -b 'KEY=VALUE' #set cookie